[project]
name = "data"
version = "0.1.0"
description = "raw data for pydantic-llm-pricing"
readme = "README.md"
# Do not publish this package to PyPI
classifiers = ["Private :: do not release"]
requires-python = ">=3.9"
dependencies = ["httpx>=0.28.1", "pydantic>=2.11.7", "pyyaml>=6.0.2"]
